---
title: 'identifying distributions '
author: "Adam Dede"
date: "2024-07-23"
output:
  html_document:
    toc: yes
    toc_float: yes
editor_options:
  chunk_output_type: console
---


## set environment

```{r message = FALSE, warning = FALSE}

# Set rendering parameters
knitr::opts_chunk$set(message = FALSE, warning = FALSE)

courseThemePath = 
  "C:\\Users\\Adam\\Documents\\GitHub\\MasterStatsUsingR\\courseTheme.R"
gitHubPath = 'C:\\Users\\Adam\\Documents\\GitHub\\MasterStatsUsingR\\data\\'
library(tidyverse) # data manipulation and visualization
source(courseThemePath)


```
## human EEG data 

```{r EEG}

df = read.csv(paste0(gitHubPath, 'restingEEG.csv'))

df$dummy = as.factor(1) 

ggplot(df, aes(x = pow_delta_O, color = dummy, fill = dummy)) + 
  geom_histogram(breaks = seq(0, 100, 1)) + 
  myTheme + brightFil + brightCol + theme(legend.position = 'none')

```
## my emails per day 

```{r EEG}

df = read.csv(paste0(gitHubPath, 'emailTimes.csv'))

df <- df %>% group_by(DayOfWeek, Month, DayOfMonth, Year) %>% 
  summarize(count = n())

df$dummy = as.factor(1) 

ggplot(df, aes(x = count, color = dummy, fill = dummy)) + 
  geom_histogram(breaks = seq(0, 51, 1)) + 
  myTheme + brightFil + brightCol + theme(legend.position = 'none')

```
## sp 500 daily change 

```{r sp 500}

df = read.csv(paste0(gitHubPath, 'SP500HistoricalData.csv'))
names(df)[6] <- 'Change'

df$Change = unlist(lapply(df$Change, function(x) as.numeric(strsplit(x, '%')[[1]])))

df$dummy = as.factor(1) 

ggplot(df, aes(x = Change, color = dummy, fill = dummy)) +
  geom_histogram(breaks = seq(-2, 2, .1)) + 
  myTheme + brightFil + brightCol + theme(legend.position = 'none') + 
  xlab('Daily Change (%)')

```

